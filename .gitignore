# Never commit secrets or local state.
.env
secrets/*
!secrets/.gitkeep
*.key
*.pem
wireguard/*.conf
!wireguard/*.tmpl

# Local setup state (the teardown manifest written by `make setup`).
.airlocked/

# macOS
.DS_Store
